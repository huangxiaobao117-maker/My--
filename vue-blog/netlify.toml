[build]
  command = "npm ci && npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173

[build.environment]
  NODE_VERSION = "20"
  NODE_OPTIONS = "--max_old_space_size=4096"
